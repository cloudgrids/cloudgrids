import { ComingSoon } from '@/components/ComingSoon';

export const metadata = {
	title: 'downflux',
	description: 'Modular TypeScript media extraction and download toolkit. Download content from websites along with structured metadata.'
};

export default function DownfluxPage() {
	return (
		<ComingSoon
			name='downflux'
			description='A modular TypeScript media extraction and download toolkit. Allows developers to download content from websites along with structured metadata — built for automated content workflows and download pipelines.'
			phase='Phase 4'
		/>
	);
}
